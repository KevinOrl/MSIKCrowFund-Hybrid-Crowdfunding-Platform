<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#E7CF8A"
    android:padding="16dp">

    <!-- Información del proyecto (vistas existentes) -->

    <!-- Nombre del proyecto -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nombre del Proyecto"
        android:textStyle="bold"
        android:textSize="18sp"
        android:paddingTop="8dp" />

    <EditText
        android:id="@+id/project_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"/>

    <!-- Descripción del proyecto -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Descripción"
        android:textStyle="bold"
        android:textSize="18sp"
        android:paddingTop="8dp" />

    <EditText
        android:id="@+id/project_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"/>

    <!-- Categoría -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Categoría"
        android:textStyle="bold"
        android:textSize="18sp"
        android:paddingTop="8dp" />

    <EditText
        android:id="@+id/project_category"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"/>

    <!-- Monto recaudado -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Monto Recaudado"
        android:textStyle="bold"
        android:textSize="18sp"
        android:paddingTop="8dp" />

    <EditText
        android:id="@+id/project_recaudado"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"/>

    <!-- Fecha límite -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Fecha Límite"
        android:textStyle="bold"
        android:textSize="18sp"
        android:paddingTop="8dp" />

    <EditText
        android:id="@+id/project_fecha"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"/>

    <!-- Dinero objetivo -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dinero Objetivo"
        android:textStyle="bold"
        android:textSize="18sp"
        android:paddingTop="8dp" />

    <EditText
        android:id="@+id/project_objetivo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"/>

    <!-- Botones -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="20dp"
        android:gravity="center">

        <Button
            android:id="@+id/donate_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Donar"
            android:visibility="gone" /> <!-- Ocultamos inicialmente -->

        <Button
            android:id="@+id/edit_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Editar"
            android:visibility="gone" /> <!-- Ocultamos inicialmente -->
    </LinearLayout>

    <!-- Botón para regresar -->
    <Button
        android:id="@+id/back_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Regresar"
        android:layout_gravity="center"
        android:layout_marginTop="10dp"/>

    <!-- Sección de comentarios -->
    <TextView
        android:id="@+id/comments_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Comentarios"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="10dp" />

    <!-- RecyclerView para mostrar los comentarios -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/comments_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp" />

    <!-- Campo para nuevo comentario -->
    <EditText
        android:id="@+id/comment_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Escribe un comentario"
        android:layout_marginTop="16dp" />

    <!-- Calificación para el comentario -->
    <Spinner
        android:id="@+id/comment_rating"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:entries="@array/rating_array" />

    <!-- Botón para publicar el comentario -->
    <Button
        android:id="@+id/post_comment_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Publicar comentario"
        android:layout_marginTop="8dp" />

</LinearLayout>
