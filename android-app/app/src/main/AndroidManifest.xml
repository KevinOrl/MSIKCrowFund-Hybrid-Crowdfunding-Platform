<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RSP2"
        tools:targetApi="31">

        <!-- Esta es la actividad que se muestra al inicio (lista de proyectos) -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declarar la actividad que muestra los detalles del proyecto -->
        <activity android:name=".VProject" />

        <!-- Otras actividades que puedas tener en tu aplicación -->
        <activity android:name=".LProject" />

        <!-- Otras actividades que puedas tener en tu aplicación -->
        <activity android:name=".Donar" />
        <activity android:name=".AdminActivity"></activity>
        <activity android:name=".SignupActivity"></activity>
        <activity android:name=".AdminDonationsActivity"></activity>
        <activity android:name=".EstadisticasActivity"></activity>
        <activity android:name=".ProjectListActivity"></activity>
        <activity android:name=".UserListActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".NewProject"></activity>
        <activity android:name=".EditProject"></activity>
        <activity android:name=".MyDonations"></activity>
        <activity android:name=".Profile"></activity>
        <activity android:name=".EditProfile"></activity>


    </application>

</manifest>